# Go build artefacts
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binaries
*.test

# Test / coverage output
*.out
coverage.txt

# Vendor / go workspace
vendor/
!nufs-core/cmd/metad/static/js/vendor/
go.work
go.work.sum

# Coverage tooling
cover.out
cover.html

# Build output directories
nufs-core/bin/

# macOS
.DS_Store

# Editor
.idea/
.vscode/
*.swp
*.swo
*~

# Local config / secrets
.env
.env.local
*.local

# Local Go build binaries produced by `go build ./cmd/<name>` without `-o`.
/metad
/datanode

# Non-project directories
codex-pet-run/

# Brainstorming visual companion mockups
.superpowers/

# binary placed at nufs-core/<name> when built from nufs-core/
nufs-core/fusegw
nufs-core/nufs-fuse
nufs-core/nufs-s3
nufs-core/nufs-cli
nufs-core/metad

# Raft runtime checkpoints
nufs-core/metadata/raft-checkpoint-*/

# IDE / tooling directories
.codegraph/
.mimocode/

# Build output for nufs-admin
nufs-admin/bin/
nufs-admin/web/node_modules/
nufs-admin/web/dist/

# Repo-root build output (e.g. binaries copied to ./bin by scripts/deploy)
/bin/

# Personal documents (kept in workspace, not committed)
*.docx
convert_to_docx.py
DEFECTS_ANALYSIS_*.md
NON_GATEWAY_ANALYSIS.md
PROBLEM_ANALYSIS.md
社会研究方法课程论文.*

# Verification / browser screenshots (transient artifacts, not source)
*.png
!nufs-core/cmd/metad/static/**/*.png
!nufs-core/datanode/admin/**/*.png
.playwright-mcp/
.claude/
