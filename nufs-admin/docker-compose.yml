version: '3.8'

services:
  admin-server:
    build: .
    ports:
      - "8090:8090"
    volumes:
      - ./deploy/clusters.yaml:/app/deploy/clusters.yaml
      - ./deploy/users.yaml:/app/deploy/users.yaml
    environment:
      - NUFS_ADMIN_CONFIG=/app/deploy/clusters.yaml